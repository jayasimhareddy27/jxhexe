{
  "extends": "./.wxt/tsconfig.json",
  "compilerOptions": {
    "allowJs": true,           // Allows JS files to be imported
    "paths": {
    },
    "types": ["chrome", "node"],

    "allowImportingTsExtensions": true,
    "jsx": "react-jsx"
  }
}
